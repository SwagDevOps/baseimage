#!/usr/bin/env sh

run() {
    (
        set -eu

        cd /build/vendor/ylem
        gem build *.gemspec
        gem install *.gem \
            -ENn /usr/local/bin --no-post-install-message \
            --no-wrappers \
            --conservative --minimal-deps

    )
}

run
