#!/usr/bin/env sh

run() {
    set -eux

    apk add --no-cache \
        bash bash-completion busybox file \
        curl sed tar grep shadow rsync git \
        htop vim less coreutils sed procps \
        ruby ruby-bundler
}

run
