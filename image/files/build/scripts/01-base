#!/usr/bin/env sh

run() {
    set -eux

    apk add --no-cache \
        bash bash-completion htop \
        busybox file \
        curl sed tar grep shadow pwgen rsync git \
        vim less coreutils sed procps \
        dropbear \
        ruby ruby-bundler ruby-json
}

run
