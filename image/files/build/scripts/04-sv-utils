#!/usr/bin/env sh

run() {
    (
        set -eu

        cd /build/vendor/sv-utils
        gem build *.gemspec
        gem install *.gem \
            --norc \
            -ENn /usr/local/bin --no-post-install-message \
            --no-user-install \
            --clear-sources \
            --no-wrappers \
            --conservative --minimal-deps
    )
}

run
