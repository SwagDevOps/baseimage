#!/usr/bin/env ruby
# frozen_string_literal: true

require 'fileutils'
self.extend(FileUtils::Verbose)

[
  '/root/.gem/',
  '/var/cache/apk/*',
  '/build',
  '/etc/profile.d/color_prompt'
].map { |g| Dir.glob(g) }.flatten.sort.each { |fp| rm_rf(fp) }
