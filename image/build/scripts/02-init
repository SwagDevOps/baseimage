#!/usr/bin/env sh

BUILD_RESULT_DIR=/build/out

# @see https://github.com/Yelp/dumb-init
main() {
    (
        set -eu

        cd "${BUILD_VENDOR_DIR}/dumb-init"
        make
        chmod +x dumb-init
        mv -v dumb-init /sbin/init
    )
}

main
