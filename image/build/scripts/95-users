#!/usr/bin/env ruby
# frozen_string_literal: true

require 'build'

Build::UserManager.new.tap do |u|
  'root'.tap { |user| u.change_shell(user, 'bash') }

  'syslog'.tap do |group|
    u.create_system_group(group)
    u.apply_perms('/var/log', "root:#{group}", 0o775)
  end
end
